#include<stdio.h>

int main()
{
    // int a;   // int uses for the intiger values to storing
    // a = 1;

    int a = 1;    //Also write this type


float b = 10.5; //float uses for the decimal values to storing 

float d;
d = 5.2;



char c = 'G';  
//To store the charecture in is.


printf("to print the size of int %d\n" ,sizeof(int));
printf("to print the size of float %d\n" ,sizeof(float));
printf("to print the size of char %d\n" ,sizeof(char));
return 0;
}

